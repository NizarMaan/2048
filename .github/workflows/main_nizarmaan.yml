name: Deploy .NET API

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.x"

      - name: Publish
        run: dotnet publish 2048.Server/2048.Server.csproj -c Release -o ./publish

      - uses: actions/upload-artifact@v4
        with:
          name: app
          path: publish/

  deploy:
    runs-on: windows-latest
    needs: build

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: app
          path: ./publish

      - name: ZIP publish folder
        run: powershell Compress-Archive -Path ./publish/* -DestinationPath ./app.zip

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_0F613EB37CE1499996818BAC64095789 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_B92876537BAC448A92EC9105B1EC64D6 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_2834ADE81C864489B6B368ED7E04D1AF }}

      - name: Deploy
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'NizarMaan'
          slot-name: 'Production'
          package: ./app.zip
